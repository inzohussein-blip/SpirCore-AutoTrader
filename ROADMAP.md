# 🗺️ ROADMAP

حالة المشروع والخطوات القادمة. / Project status and what's next.

## ✅ المنجز (Done)
- **Phase 1 — MQL5 Core**: تنفيذ ECN، فلتر سبريد، خطوط مستقبلية، لوحة أزرار.
- **محرك الاستراتيجيات**: CEZLSMA (اتجاه)، BBRSI (ارتداد)، LRCUTB (زخم).
- **إدارة المخاطر**: حد خسارة يومي، حد صفقات/يوم، تحجيم بالمخاطرة (EA + الجسر).
- **إدارة الصفقات**: break-even + trailing (EA + حلقة خلفية في الجسر).
- **القياس**: سجل صفقات CSV + إحصاءات حية + `analyze.py` (تقرير أداء) + `backtest.py` (باك-تِست وترتيب).
- **Phase 2 — Python Bridge**: FastAPI + WebSocket + مكتبة MetaTrader5.
- **Phase 3 — Chrome Extension**: التقاط تنبيهات TradingView + وضعان (Bridge / MT5 Web) + محدّدات قابلة للتخصيص.

## 🔜 الجلسة القادمة — واجهة عمل للمشروع (Dashboard UI)
الهدف: **واجهة تحكم مركزية واحدة** بدل الأزرار المبعثرة، تعرض كل شيء وتتحكم بكل شيء.

### النطاق المقترح
- **لوحة حية (Web Dashboard)** تُخدَّم من جسر Python (FastAPI + صفحة HTML/JS واحدة):
  - حالة الاتصال (MT5 / الجسر / الإضافة) ورصيد الحساب والسبريد اللحظي.
  - الصفقات المفتوحة (لوت، ربح عائم، SL/TP) مع أزرار إغلاق فردي/كلي.
  - **منحنى رأس المال (Equity curve)** وإحصاءات اليوم/الأسبوع (من السجل).
  - التحكم بالأتمتة (ON/OFF)، اختيار الاستراتيجية، وتعديل المخاطر لحظياً.
  - سجل أحداث حي (إشارات، تنفيذ، حظر مخاطر).
- **واجهة برمجية داعمة** في `server.py`: نقاط `GET /positions`, `GET /journal`, `POST /control` (تفعيل/تعطيل، تغيير إعدادات) عبر WebSocket للتحديث اللحظي.
- **تصميم**: صفحة واحدة متجاوبة، ثيم داكن/ذهبي متسق مع الإضافة، بلا تبعيات ثقيلة.

### قرارات مطلوبة قبل البدء (نحسمها بداية الجلسة القادمة)
1. مكان الواجهة: صفحة تُخدَّم من الجسر (موصى به) أم تطبيق منفصل؟
2. هل تريد تحكماً بالـ EA من الواجهة (يتطلب جسر أوامر عكسي Python→EA عبر ملف/سوكيت)؟
3. مصادقة الواجهة (توكن) بما أنها ستتحكم بالتداول.

## 🧭 أفكار أبعد (Backlog)
- استراتيجيات إضافية من `geraked/metatrader5` (NWERSIASF, 3MACD, ...).
- Walk-forward optimization + تقرير HTML من `backtest.py`.
- إشعارات (Telegram / بريد) عند الإشارات والتنفيذ وحظر المخاطر.
- تحسين محاكاة الباك-تِست (نمذجة الانزلاق، تعدد الصفقات، السبريد المتغيّر).

> تذكير دائم: الأرقام هي الحَكَم. لا تشغيل حقيقي قبل Demo طويل + باك-تِست بـ 100+ صفقة وPF مستقر.
